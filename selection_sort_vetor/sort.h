#ifndef SORT_VECTOR_HEADER

#define SUCESSO 0
#define FALHA 1
#define INVALIDO -1

int selectionSortCresc(int * lista, unsigned int len);
int selectionSortDecresc(int * lista, unsigned int len);

#endif // !SORT_VECTOR_HEADER